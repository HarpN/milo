apiVersion: v1
kind: Service
metadata:
  name: {{ include "milo.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "milo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app: milo-guide-scraper
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: grpc
